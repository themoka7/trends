# 정책 동향 피드 — 기획서

작성일: 2026-08-07 · v3 (최종)
이전: `기획서_정부정책_AI피드.md` (v1 소스 검토 → v2 방향 전환)

> **참고 자료**: `7주차_공모전접수_확정구조_고려사항_v2.md`, `Airtable웹훅_GAS연동_세팅주의사항_v2.md`
> 두 문서의 **구조(Vercel 서버 경유 · Airtable 저장 · GAS 메일)와 운영 함정**을 그대로 차용한다.
> 공모전은 만들지 않는다.

---

## Ⅰ. 무엇을 만드는가

정부·공공기관의 행정문서 흐름을 매일 관측해, **정책이 발표되기 전 단계의 신호**를 찾아 알려주는 사이트다.
방문해서 볼 수도 있고, 메일로 받아볼 수도 있다.

```
 [GitHub Actions]  새벽 배치 — 수집 · 필터 · AI 분석
        │
        ▼
 [Airtable]  신호와 구독자가 쌓이는 표
        │                        │
        │                        └─→ [웹훅] → [GAS] → 구독자에게 메일
        ▼
 [Vercel]  화면 + 서버
        │   화면   오늘의 신호 / 신호 상세 / 구독 신청
        └─  서버   ← 열쇠(API 키)는 여기에만. 읽기도 쓰기도 전부 여기를 거친다
```

**핵심 가치 제안**
> 정책브리핑은 *발표된 것*을 알려준다. 이 사이트는 *준비 중인 것*을 알려준다.

---

## Ⅱ. 도구가 하나씩 맡는다

| 도구 | 맡는 일 | 맡지 않는 일 |
|---|---|---|
| **GitHub Actions** | 새벽 배치 — 수집 → 필터 → AI → Airtable 기록 | 화면을 띄우지 않는다 |
| **Vercel** | 화면을 띄우고, 뒤에서 열쇠를 쥔 채 Airtable을 부른다 | **데이터를 저장하지 않는다. 무거운 배치를 돌리지 않는다** |
| **Airtable** | 신호 · 구독자 · 발송 이력이 쌓이는 표 | 화면을 만들지 않는다. **원본 문서를 통째로 담지 않는다** |
| **GAS** | 메일 발송 | 화면도 수집도 하지 않는다 |
| **Gemini API** (무료 등급) | 문서 **묶음**을 읽고 신호 해석 초안 | 최종 판단을 하지 않는다. **1건씩 부르지 않는다** |
| **Tailwind · shadcn/ui** | 화면 부품 | 로직에 관여하지 않는다 |

### 왜 배치를 Vercel이 아니라 GitHub Actions에서 돌리는가

Vercel 서버 함수는 **실행 시간 제한**이 있다. 수만 건 수집 + AI 호출은 그 안에 끝나지 않는다.
GitHub Actions는 스케줄 실행이 되고 실행 시간이 넉넉하며 무료다. **배치는 Actions, 화면은 Vercel**로 나눈다.

### 왜 Airtable에 원본을 담지 않는가

Airtable은 레코드 수 제한이 빡빡하다(무료 base당 1,000건). 일일 수만 건의 원본 문서를 넣으면 **하루 만에 터진다.**

| | 건수 | 무료 한도 1,000건 대비 |
|---|---|---|
| 발행 신호 | 월 150~450건 | 여유 있음 |
| 수집 원본 | **일 수만 건** | **하루 만에 초과** |

Airtable의 진짜 가치는 *"사람이 브라우저에서 보고 체크박스를 누르는 표"* 다. `published` 검수가 정확히 그 용도다.
**데이터베이스로 쓰지 않는다.**

---

## Ⅱ-2. 데이터는 어디에 저장하는가

### 요구사항: 히스토리는 필요하지만, 전건 레코드는 필요 없다

이 서비스의 핵심 신호는 **"평소 대비 급증"** 이다. 오늘 문서만 봐서는 급증인지 알 수 없고,
최소 6~12개월 히스토리가 있어야 기준선이 나온다.

**다만 기준선에 필요한 것은 집계 숫자이지 문서 레코드가 아니다.**

| 필요한 것 | 형태 | 쓰임 |
|---|---|---|
| 키워드별 시계열 | `키워드 × 월 × 기관 → 건수` | "평소 주 2건 → 이번 주 8건" |
| 전체 문서량 보정치 | `일자 × 기관 → 총건수` | 연말처럼 전반적으로 문서가 늘 때 오탐 방지 |
| 과거 키워드 사전 | 키워드 목록 | "12개월간 없던 키워드" 판정 |

「근무명령부」 3만 건을 저장해봐야 어떤 클러스터에도 들어가지 않고 기준선에도 기여하지 않는다.
→ **전건은 훑되 저장하지 않는다. 카운터만 올리고 버린다.**

### 계층

| 계층 | 무엇 | 어디에 | 연간 규모 |
|---|---|---|---|
| **L0** | 일자 × 기관 총건수 카운터 | **SQLite (Turso)** | 약 18만 행 / 수십 MB |
| **L0b** | 키워드 × 월 × 기관 집계 · 키워드 사전 | **SQLite (Turso)** | 수만 행 |
| **L1** | **필터 통과 문서만** — 담당부서 · 단위업무 · 보존기간 포함 | **SQLite (Turso)** | 일 3,000건 → 연 110만 건 / **약 440MB** |
| **L2** | 신호 · 구독자 · 발송이력 | **Airtable** | 월 150~450건 |

**연 500MB 내외 → 무료 5GB로 10년 이상 간다.**

### 수집 단계에서 이미 걸러낸다

```
목록 페이지 훑기 (전건)  ──→  카운터 +1  ──→  버림
                         └─→  룰 필터 통과분만  ──→  상세 조회  ──→  저장
```

제목을 봐야 필터링이 되므로 **목록 페이징 자체는 전건을 훑어야 한다.**
그러나 **저장은 통과분만, 상세 조회도 통과분만** 한다. 요청량과 저장량이 함께 줄어든다.

**Turso를 고른 이유** — 무료 5GB / 월 10억 row read, SQLite 호환이라 로컬 개발과 동일하고,
HTTP 접근이라 GitHub Actions에서 바로 쓴다.

| 대안 | 왜 안 골랐나 |
|---|---|
| Supabase · Neon | 무료 0.5GB — 부족 |
| SQLite 파일을 Git에 커밋 | 바이너리라 매일 커밋하면 히스토리가 폭발 |
| Cloudflare D1 | 무료 조건은 좋으나 Vercel과의 조합이 어색 |

### 누가 어디에 접근하는가

```
GitHub Actions  →  Turso (읽기 + 쓰기)  →  Airtable (신호만 쓰기)
Vercel          →  Airtable (신호 읽기 / 구독자 쓰기)
GAS             →  Airtable (읽기 + 발송이력 쓰기)
```

**Vercel은 Turso를 보지 않는다.** 신호를 만들 때 근거 문서 목록을 JSON으로 `source_docs` 필드에
박아두면 화면은 Airtable만 읽으면 된다. 접근 경로가 하나 줄어든다.
(Phase 2에서 "기관별 추적" 같은 기능을 넣으면 그때 Vercel → Turso 읽기 경로를 추가한다.)

### 용량 — 저장이 아니라 훑는 양이 변수다

저장량은 **필터 통과분**에만 비례하므로 관리된다. 아직 모르는 것은 일일 유입 건수인데,
이것은 저장 용량보다 **수집 시간과 요청 횟수**에 영향을 준다.

| 시나리오 | 일 유입 | 통과분(10% 가정) | 연간 저장 | 판단 |
|---|---|---|---|---|
| 낙관 | 3만 건 | 3,000건 | 약 440MB | 무료 5GB로 10년 |
| 비관 | 35만 건 | 35,000건 | 약 5GB | 필터 강화 또는 수집 범위 축소 |

목록 화면에 `10,585,271건`이 떴으나 전체 누적인지 최근 1개월치인지 불분명하다.
→ **S2(일일 건수 측정)** 에서 확정한다. 비관 쪽이면 §ⅩⅡ-3의 *"중앙행정기관 + 주요 공공기관 한정"* 을 적용한다.

### 보존 정책

- **최근 12개월** — 필터 통과 문서를 문서 단위로 보관
- **12개월 초과** — `키워드 × 기관 × 월` 집계만 남기고 문서 삭제
- 카운터 · 키워드 사전은 계속 보관 (용량이 미미하고 기준선의 근거)

---

## Ⅲ. 두 갈래 흐름

### 흐름 ① 배치 — 신호를 만든다 (매일 새벽)

```
02:00  [수집]    open.go.kr 전일 신규 목록 페이징
                 → 룰 필터 통과분만 상세 조회
02:30  [정제]    ⚠️ 담당자명 즉시 폐기 → 중복 제거 → 정규화
03:00  [룰 필터]  정형 반복문서 제외 (약 90% 감소)
03:30  [클러스터] 키워드 · 기관 · 부서 · 시계열 군집화 (코드, AI 아님)
04:00  [이상탐지] "평소 대비 급증" 통계 신호 추출
04:30  [AI 해석]  유의미 클러스터에 이름 · 맥락 · 영향 (Claude)
05:00  [기록]    Airtable `신호` 테이블에 5~15행 추가
```

### 흐름 ② 구독 — 신호를 보낸다

```
사용자가 /subscribe 에서 메일 + 관심 태그 제출
        ↓
Vercel 서버 → Airtable `구독자` 추가 (상태: 확인대기)
        ↓
확인 메일 발송 → 사용자가 링크 클릭 → 상태: 활성      ← 더블 옵트인
        ↓
        ⋯
        ↓
매일 07:00  Airtable 웹훅 → GAS → 관심 태그가 맞는 활성 구독자에게 발송
        ↓
       `발송이력`에 기록 (중복 발송 방지)
```

> **메일은 아침 7시에 보낸다.** 배치는 새벽에 끝나지만 발송은 아침에 한다.
> 새벽 메일은 열람률이 낮고, 야간 발송은 오해를 살 수 있다(§Ⅵ-2).

---

## Ⅳ. Airtable 스키마

필드 이름은 **영문**으로 만들고 화면 표시만 한글로 한다. (참고 문서 §Ⅵ 권장을 따름)

### 테이블 `signals` — 발행 신호

| 필드 | 형태 | 채우는 주체 |
|---|---|---|
| `signal_id` | 텍스트 (고유) | 배치 |
| `title` | 한 줄 — 신호 제목 | AI |
| `summary` | 여러 줄 — 무슨 일이 벌어지고 있는가 | AI |
| `category` | 선택지 (고정 목록) | AI |
| `signal_type` | 선택지 — 동시다발/급증/신규등장/상급지시/예산선행 | 코드 |
| `strength` | 숫자 1~5 — **정수** | 코드 (통계) |
| `doc_count` | 숫자 — 근거 문서 수 | 코드 |
| `org_count` | 숫자 — 관련 기관 수 | 코드 |
| `source_docs` | 여러 줄 — 근거 문서 제목+링크 JSON | 코드 |
| `detected_at` | 일시 | 배치 |
| `published` | 체크박스 | **사람** (검수 후 공개) |
| `sent_at` | 일시 | GAS |

> **`strength`(통계 기반)와 `published`(사람 검수)를 나누는 것이 이 설계의 핵심이다.**
> 참고 문서가 *"AI 평점과 담당자 확정 평점을 다른 칸으로 나눈다"* 고 한 것과 같은 이유다.
> 칸이 나뉘어 있다는 사실만으로 "AI가 낸 건 초안"이라는 성격이 남는다.

### 테이블 `subscribers` — 구독자

| 필드 | 형태 |
|---|---|
| `email` | 한 줄 (고유) |
| `tags` | 다중 선택 — 관심 분야 |
| `status` | 선택지 — `pending` / `active` / `unsubscribed` |
| `confirm_token` | 한 줄 — 더블 옵트인 · 수신거부 링크용 |
| `subscribed_at` / `confirmed_at` | 일시 |

### 테이블 `send_log` — 발송 이력

| 필드 | 형태 |
|---|---|
### ~~테이블 `send_log`~~ — **만들지 않는다**

발송 이력을 `signal_id × email` 조합으로 남기면 **신호 10건 × 구독자 500명 = 하루 5,000행**이 되어
Airtable 무료 한도(1,000 레코드)를 하루 만에 넘긴다.

→ **`signals.sent_at` 하나로 통합한다.** "이 신호를 발송했는가"만 알면 중복 방지가 되고,
이것이 참고 문서의 `발송일시` 패턴이기도 하다. 신호당 1행이므로 월 150~450건으로 유지된다.

> ⚠️ **중복 방지 필드명은 `sent_at` 하나로 통일한다.**
> 참고한 두 문서가 `발송일시`와 `처리일시`로 어긋나 있었다. 그대로 두면 중복 방지가 무력화되어
> 같은 메일이 여러 통 나간다. 증상이 "가끔 두 번 온다" 라서 추적도 어렵다.

---

## Ⅴ. 화면

```
 /                    오늘의 신호 — 카드 목록              [데이터 O]
 /signal/[id]         신호 상세 — 근거 문서 전체 목록        [데이터 O]
 /subscribe           구독 신청 폼
 /subscribe/confirm   구독 확인 (더블 옵트인 랜딩)
 /unsubscribe         수신 거부
 /about               서비스 소개 · 분석 방법 · AI 한계 고지  [글만]
 /privacy             개인정보처리방침                      [글만]
```

**데이터를 읽어와 그리는 화면은 2개(`/`, `/signal/[id]`)로 묶는다.** 나머지는 글만 있는 페이지다.
참고 문서의 *"데이터 화면이 두 개를 넘어가면 시간 안에 끝나지 않는다"* 원칙을 따른다.

### 신호 카드에 반드시 들어가야 하는 것

```
┌─────────────────────────────────────────┐
│ [공급망]  ●●●○○  신호 강도 3            │
│                                         │
│ 중동 수입 의존 품목 대체공급처 점검 확산  │
│                                         │
│ 8월 첫째 주, 3개 기관에서 관련 문서 7건  │
│ 발생. 평소 대비 4배.                     │
│                                         │
│ 근거 문서 7건 보기 →                     │
│ ⓘ AI 분석 초안입니다. 원문을 확인하세요.  │
└─────────────────────────────────────────┘
```

**타협 불가 3가지** — 제목 기반 추론은 본질적으로 불확실하므로, 이게 있어야 신뢰를 얻는다.
1. **근거 문서 원문 링크** 상시 노출
2. **신호 강도 수치** (문서 수 · 기관 수 · 급증률) 명시
3. **"AI 분석 초안" 고지**

---

## Ⅵ. 지켜야 할 것

### 1. 열쇠는 서버에만 — **읽기도 포함한다**

참고 문서 §Ⅴ-1은 주로 *쓰기*를 말하지만, 이 사이트는 **읽기가 더 많다.**
`/` 와 `/signal/[id]` 는 Airtable을 읽어야 하는데, 화면에서 직접 `fetch` 하면 토큰이 그대로 노출된다.

- 모든 Airtable 접근은 **Vercel 서버 라우트를 거친다**
- 화면은 서버 렌더 + **60초 캐시(`revalidate`)** — 방문할 때마다 Airtable을 부르지 않는다
- 환경변수는 **내 컴퓨터와 Vercel 두 곳 모두**에 넣는다 (참고 문서 §Ⅵ 최다 빈발 문제)

### 2. 메일은 동의받고 보내고, 언제든 끊을 수 있어야 한다

- **더블 옵트인** — 메일 입력만으로 발송하지 않는다. 확인 링크를 눌러야 활성화된다.
  (남의 메일 주소를 넣는 장난·오타 방지)
- **모든 메일 하단에 수신거부 링크** — 클릭 한 번에 끊긴다. 로그인 요구 금지
- **발송 시각은 아침 7시**
- 정책 정보는 광고성 정보가 아니지만, 위 셋은 갖춰두는 편이 안전하다

### 3. 개인정보는 두 겹이다

| 대상 | 처리 |
|---|---|
| **수집 문서의 담당자 실명** | ⚠️ 파싱 즉시 폐기. **DB에 저장하지 않는다** |
| 제목 속 인명 | 마스킹 |
| **구독자 이메일** | Airtable에만. 화면·메일 본문에 다른 구독자 정보 노출 금지 |

open.go.kr 상세 페이지에는 담당자 실명이 그대로 노출된다(실측 확인). **이것이 이 프로젝트의 가장 실질적인 법적 위험 지점이다.**

### 4. 사람이 검수한 것만 나간다

배치가 만든 신호는 `published` 체크 전까지 화면에도 메일에도 나가지 않는다.
AI가 헛다리를 짚었을 때 막을 수 있는 유일한 지점이다.

### 5. 수집은 예의 있게

| 조치 | 내용 |
|---|---|
| 요청 간격 | 최소 1~2초, 동시 요청 1 |
| 시간대 | 새벽 |
| User-Agent | 서비스명·연락처 명시 (숨기지 않음) |
| 캐싱 | 같은 문서 재요청 안 함 |
| 상세 조회 | 룰 필터 통과분만 (요청량 1/10 이하) |
| 차단 시 | **즉시 중단**하고 기관에 정식 문의 |

robots.txt는 법적 구속력이 없고 정보공개법상 공개 의무 데이터지만, **가장 현실적인 위협은 IP 차단으로 인한 서비스 중단**이다.

---

## Ⅶ. 자주 막힐 곳

참고 문서 §Ⅵ의 형식을 이 프로젝트에 맞춰 옮긴 것이다.

| 증상 | 대개 이것 | 확인 순서 |
|---|---|---|
| 배포했는데 화면이 그대로 | 빌드 중 / 캐시 | 1분 대기 → `Ctrl+Shift+R` → `revalidate` 값 확인 |
| **내 컴퓨터에선 되는데 배포하면 안 됨** | **환경변수 한 곳만 넣음** | 내 컴퓨터 + Vercel **두 곳** 모두 확인 |
| 배치는 돌았는데 Airtable에 없음 | 토큰 권한 / 필드명 불일치 | ① 토큰에 그 base 포함 ② 필드명 **글자까지** 같은지 |
| **신호 강도 저장 실패** | **0 또는 소수점** | 1~5 정수만. 값이 없으면 빈칸 |
| 구독 신청은 되는데 확인 메일이 안 옴 | GAS 권한 미승인 | 편집기에서 함수 1회 실행 → 승인 |
| **메일이 두 번 이상 옴** | `send_log` 중복 체크 누락 | `signal_id + email` 조합 확인. **`LockService`로 감싼다** |
| **처음엔 되다가 며칠 뒤 멈춤** | **GAS가 값을 반환하고 있다** | `doPost`에서 정상 시 `return` 금지 → 200. 302면 13회 후 웹훅 비활성 |
| 코드 고쳤는데 반영 안 됨 | 재배포로 주소가 바뀜 | 기존 배포를 **새 버전으로 갱신**해 주소 유지 |
| 어느 날부터 신호 없음 | 웹훅 7일 만료 | 하루 1회 연장 트리거 확인 |
| 갑자기 AI 호출이 폭증 | 자동 재시도 / 배치 중복 실행 | **자동 재시도 켜지 않는다.** Actions 동시 실행 방지 |
| 수집이 갑자기 0건 | 사이트 구조 변경 or IP 차단 | 브라우저로 직접 접속해 확인 |

---

## Ⅷ. 비용

| 항목 | 초기 | 비고 |
|---|---|---|
**전부 무료 티어로 간다.** 각 한도와 우리 사용량은 다음과 같다.

| 항목 | 서비스 | 무료 한도 | 우리 사용량 | 판정 |
|---|---|---|---|---|
| **AI** | Gemini API 무료 등급 | 일일 요청 수 제한 (⚠️ 아래) | **묶어 보내면 약 60요청/일** | ✅ |
| **DB** | Turso | 5GB · 월 5억 row read · DB 100개 | 연 약 440MB | ✅ 넉넉 |
| 호스팅 | Vercel Hobby | 무료 | 정적 + 간단 API | ✅ 비상업 한정 |
| 배치 | GitHub Actions | **퍼블릭 무제한** / 프라이빗 월 2,000분 | 일 1~2시간 | ✅ **퍼블릭 채택** |
| 표 · 검수 | Airtable | base당 1,000 레코드 | 월 150~450건 | ⚠️ 아카이브 필요 |
| 메일 | GAS | 개인 하루 100명 | 구독자 수 | ⚠️ **100명이 천장** |
| 도메인 | `*.vercel.app` | 무료 | | ✅ |

Turso와 Cloudflare D1은 둘 다 5GB지만, **D1은 일 단위 리셋(읽기 500만 / 쓰기 10만)** 이고
Turso는 **월 5억 row read** 라 새벽에 몰아 도는 배치에 더 여유롭다. → **Turso 채택.**

### ⚠️ AI 호출은 반드시 묶는다 — 무료 등급의 핵심 제약

무료 등급은 **일일 요청 수**가 제한된다. 문서 1건당 1요청이면 즉시 한도를 넘긴다.

```
❌  문서 3,000건 × 1요청 = 3,000요청/일   →  한도 초과
✅  제목 100건을 묶어 1요청     =    30요청/일
    + 클러스터 해석                  20~30요청
    ──────────────────────────────────────────
    합계 약 60요청/일
```

**"제목 100건을 한 번에 보내고 JSON 배열로 받는다"** 가 설계 전제다.
이는 비용 절감이 아니라 **동작 가능 여부**의 문제다.

> Gemini 무료 등급의 정확한 일일 요청 한도는 출처마다 250~1,500으로 엇갈린다.
> 어느 쪽이든 60요청이면 통과하지만, **Google 공식 문서에서 한 번 확인**하고 시작한다.
> 무료 등급은 입력이 모델 개선에 쓰일 수 있으나, 우리가 넣는 것은 **이미 공개된 정부 문서 제목**이라 무방하다.

### Airtable 1,000 레코드 관리

- AI가 만든 신호 후보를 **전부 올리지 않는다.** 상위 N건만 Airtable, 나머지는 Turso에 둔다
- **90일 지난 신호**는 Turso로 아카이브 후 Airtable에서 삭제

### 무료로 넘을 수 없는 벽: 구독자 100명

개인 Gmail 하루 100명이 GAS의 한계다. 넘어가면
① 부산대 Workspace 계정(1,500명) 또는 ② Resend 등 전용 발송 서비스 무료 티어로 전환한다.
**초기 검증 단계에서는 문제되지 않는다.**

### ⚠️ 진짜 병목: 메일 발송 한도

| 계정 | 하루 수신자 한도 |
|---|---|
| 개인 Gmail | **100명** |
| Google Workspace | **1,500명** |

**구독자 100명을 넘는 순간 개인 계정으로는 불가능하다.**
그리고 참고 문서가 경고했듯 **기관 계정은 웹앱 공개 배포가 막혀 있을 수 있다.**

→ **Phase 0에서 가장 먼저 확인해야 할 항목이다.** (§Ⅸ-S1)
→ 구독자가 1,500명을 넘으면 Resend·SendGrid 같은 전용 발송 서비스로 이전한다. 로직은 거의 같다.

---

## Ⅸ. Phase 0 — 착수 전 검증 (1주)

- [ ] **S1.** ⚠️ **기관 계정의 GAS 웹앱 공개 배포 허용 여부 + 메일 한도 확인** ← 막히면 설계가 바뀐다
- [x] **S2.** 수집기 프로토타입 — **완료** (아래 결과)
- [x] **S3.** rate limit 임계치 — **완료** (3초 간격 + 백오프. 아래 ⚠️)
- [x] **S4.** 룰 필터 — **완료. 제거율 99%** (목표 85% 초과)
- [x] **S5.** `단위업무` 분포 분석 — **완료** (852종 롱테일 확인, 필터 축 재설계에 반영)
- [ ] **S6.** ★ **핵심 파일럿** — 1주치 데이터로 신호 탐지 + AI 해석 후 **결과물 육안 평가**
- [ ] **S7.** 담당자명 제거 로직 검증 — 100% 제거 확인
- [ ] **S8.** 공공누리(KOGL) 라이선스 유형 확인

### S2~S5 실측 결과 (2026-08-07)

**수집 경로 — 순수 HTTP는 막힌다**

목록 AJAX(`POST /othicInfo/infoList/infoList.ajax`)를 requests / curl_cffi 로 부르면
HTTP 200 에 `code: 491` 만 오고 데이터가 비어 있다. 배제한 가설:

| 가설 | 결과 |
|---|---|
| 세션 쿠키 부재 | warmup GET 후에도 491 |
| XSRF-TOKEN 헤더 누락 | 토큰을 붙여도 491 |
| TLS 핑거프린팅 | `curl_cffi(impersonate="chrome")` 로도 491 |
| 검색조건 세션 미등록 | `.do` GET/POST 선행 3가지 시나리오 모두 491 |

→ **실제 브라우저 페이지 컨텍스트에서만 동작.** WAF(elevisor)가 JS 실행 컨텍스트를 요구한다.
→ Playwright로 페이지를 1회 열고 그 안에서 fetch 루프를 돈다 (`collector/browser_client.py`).
→ 엑셀 다운로드는 **1,000건 제한**이라 대안이 되지 못한다.

**볼륨 (중앙행정기관)**

| 날짜 | 건수 |
|---|---|
| 08-03 | 46,370 |
| 08-04 | 42,135 |
| 08-05 | 40,012 |
| 08-06 | 38,148 |
| **평균** | **41,666건/일** |

전체 기관은 **454,555건/일** — 중앙행정기관 한정이 전체의 **9%** 로 줄여준다.
페이지당 200건 → **약 200요청 / 하루 약 10분**(3초 간격 기준).
GitHub Actions 프라이빗(월 2,000분)으로도 충분하다.

**⚠️ rate limit — 소량 테스트를 믿으면 안 된다**

14페이지를 1.5초 간격으로 돌렸을 때 문제가 없어 "1.5초 안정"으로 판정했으나,
**191페이지 연속 요청에서 `code: 429` 로 전량 실패**했다. 7일치가 통째로 날아갔다.

| 조건 | 결과 |
|---|---|
| 1.5초 × 14페이지 | 정상 |
| **1.5초 × 191페이지** | **429 — 전량 실패** |
| **3초 × 40페이지** | **정상** |

→ 요청 간격 **3초**로 상향.
→ 429를 만나면 **20초 → 40초 → 80초 → 160초** 지수 백오프 후 재시도하고,
   재시도 전에 **페이지를 다시 열어 세션을 갱신**한다 (쿼터가 세션 단위일 가능성).
→ 하루 실패가 다음 날 수집을 막지 않도록 날짜별로 예외를 격리한다.

**주말은 거의 비어 있다** — 08-01(토) 1,316건 / 08-02(일) 2,326건.
평일의 3~6% 수준이라 배치가 훨씬 빨리 끝난다.

**필터 — 블랙리스트가 아니라 화이트리스트**

2,800건 균등표집 분석 결과 **단위업무 852종, 부서 832개**로 롱테일이 극심했다.
상위 70종 단위업무를 다 걷어내도 누적 56%에 불과해 **블랙리스트로는 85% 도달이 불가능**했다.
중앙행정기관 문서의 대부분이 부처 본청이 아니라 **일선 기관(교도소·보훈지청·고용센터)의
개별 사건 처리 문서**이기 때문이다.

→ 기본을 배제로 두고 통과 조건을 명시하는 방식으로 전환.
→ 정책부서는 **단독 통과 조건이 아니다** — 정책부서 문서의 다수도 정형 실무였다
   (관서운영경비 지급결의 / 부속품 교체 구매 / 사업계획 변경 신고 수리).
   약한 정책 어휘와 **함께** 걸릴 때만 통과시킨다.

| 날짜 | 표본 | 통과 | 제거율 |
|---|---|---|---|
| 08-06 | 2,800 | 23 | **99.2%** |
| 08-04 | 2,800 | 31 | **98.9%** |

두 날짜가 일관되어 과적합이 아니다.

**통과분 예시 (실제 정책 신호)**

```
「북극항로 활용 촉진 및 연관산업 육성에 관한 특별법 시행령」 제정 추진계획(안)
「역사문화권 정비 등에 관한 특별법」 하위법령 개정 계획 보고
재생에너지자립도시 조성 및 지원에 관한 특별법안 검토의견 제출
「역세권법」일부개정법률안(김남준 의원 대표발의안) 의견 제출
의원발의 법률안 부처 협의 요청(성폭력방지법, 20375호)
폭염 등 피해복구 지원을 위한 전략작물직불제 운영 방안
2026년도 추석 연휴 특별교통대책 수립을 위한 추진계획
```

**규모가 다시 줄었다 — 앞선 추정보다 훨씬 여유롭다**

| | 기존 가정 | **실측** |
|---|---|---|
| 통과 건수 | 3,000건/일 | **약 380건/일** (1%) |
| AI 요청 | 약 60요청/일 | **약 25요청/일** |
| 저장 | 연 440MB | **연 약 55MB** |

Gemini 무료 등급과 Turso 5GB 모두 **한도의 발끝에도 못 미친다.**
급증 판정의 기준선은 통과분이 아니라 **전건 38,148건 기준 카운터**로 쌓으므로 모수는 충분하다.

> **S6가 이 프로젝트의 성패를 결정한다.**
> 나머지가 다 되어도 신호 결과물이 "그래서 뭐?" 수준이면 서비스가 성립하지 않는다.
> 반대로 S6에서 "어 이거 재밌는데?" 가 나오면 나머지는 전부 실행 문제다.

---

## Ⅹ. 만드는 순서

참고 문서 §Ⅲ의 원칙 — **한 번에 다 만들지 않는다. 하나 만들고 직접 확인하고 다음으로 간다.**

| 단계 | 하는 일 | 끝났다는 기준 |
|---|---|---|
| **1** | GitHub → Vercel 연결 → Airtable base → 키 발급 → 환경변수 | **빈 화면이라도 내 주소로 접속된다** |
| **2** | Airtable에 손으로 신호 3건 입력 → `/` 화면에 그리기 | 내 주소에서 신호 카드가 보인다 |
| **3** | `/signal/[id]` 상세 + 근거 문서 링크 | 카드를 눌러 상세로 들어가진다 |
| **4** | `/subscribe` 폼 → Airtable 저장 → 확인 메일 → 활성화 | **내 메일로 확인 링크가 오고 눌러서 활성화된다** |
| **5** | 수집 배치 (GitHub Actions) — 수집·필터까지만 | 새벽에 돌아서 필터 결과 파일이 생긴다 |
| **6** | 클러스터링 + 통계 신호 추출 | 신호 후보가 나온다 |
| **7** | AI 해석 → Airtable 기록 | 아침에 신호 5~15건이 `published` 미체크 상태로 쌓인다 |
| **8** | GAS 발송 (웹훅 + 중복 방지 + 연장 트리거) | **검수 체크한 신호만 내 메일로 온다** |
| **9** | 최종 점검 | 다른 사람이 구독하고 메일을 받는다 |

**4단계까지가 웹사이트고, 5단계부터가 데이터 파이프라인이다.** 4단계까지 먼저 끝내면 눈에 보이는 것이 생긴다.

---

## ⅩⅠ. 다 됐는지 확인하는 목록

- [ ] 내 배포 주소로 접속된다
- [ ] 화면 코드에 API 키가 들어 있지 않다 (**읽기 경로도 포함**)
- [ ] `/` 에 신호 카드가 보이고, 근거 문서 링크가 열린다
- [ ] 모든 신호에 **강도 수치**와 **"AI 분석 초안" 고지**가 있다
- [ ] `published` 미체크 신호는 화면에 안 보인다
- [ ] 구독 신청 → 확인 메일 → 링크 클릭 → 활성화가 된다
- [ ] 메일 하단 수신거부 링크가 **한 번에** 동작한다
- [ ] 같은 신호가 같은 사람에게 **두 번 가지 않는다**
- [ ] GAS `doPost`가 정상 시 아무것도 반환하지 않는다
- [ ] 웹훅 연장 트리거가 하루 1회 등록돼 있다
- [ ] Airtable 어디에도 **담당자 실명이 없다**
- [ ] `/privacy` 가 있고 수집·보관 항목이 적혀 있다
- [ ] 옆 사람이 구독해 봤고 다음 날 메일을 받았다

---

## ⅩⅡ. 남은 결정 사항

1. **분야(`category`) 목록 확정** — 4~8개. 자유 서술로 두면 표에서 세어지지 않는다.
   *(예: 공급망 · 산업지원 · 규제 · 예산재정 · 인력고용 · 지역 · 대외협력)*
2. **타깃 독자** — 특정 업종 실무자 / 일반 / 전문가·기자
   → 좁은 업종 타깃이 차별성·수익성 모두 유리
3. **수집 범위** — Phase 1은 **중앙행정기관 + 주요 공공기관 한정** 권장 (볼륨·차단 리스크 관리)
4. **회원가입 범위** — 비밀번호 기반 계정은 과하다. **메일 + 토큰 링크**로 구독 관리하는 방식 권장
   (관심 태그 변경도 메일 속 링크로 가능 → 가입 이탈 없음)

---

## 부록: 실측 근거

- `robots.txt` 직접 조회 — 전체 Disallow 확인
- 목록 페이지 브라우저 렌더링 — 누적 10,585,271건 (2026-08-07 기준)
- 상세 페이지 `infoListDetl2.do` GET 접근 — 전체 필드 및 **담당자 실명 노출** 확인
- 사용 가능 필드: 제목/기관명/담당부서명/생산일자/문서번호/**보존기간**/**단위업무**/공개여부/분류체계/연관정보
- 공공데이터포털 — 정보공개포털 원문정보 오픈API 부재 확인
- 모델 가격 · Batch 50% · 캐싱 배율 — Anthropic 공식 문서
- GAS 메일 한도 · 웹훅 302 함정 · 7일 만료 — 참고 문서 `Airtable웹훅_GAS연동_세팅주의사항_v2.md`
